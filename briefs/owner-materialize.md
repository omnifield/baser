# owner-materialize

**Роль:** owner-зоны. **Зона:** `packages/baser-materialize` (`@omnifield/baser-materialize`).
Работаешь только в своей папке; commit-only под git-гейтом.

**Задача:** `tasker:BASER-20` — там полное ТЗ, DoD и границы. Веди работу в узле, не здесь.

**Обязательно до первого действия:** `kb:BASER-5` (контракт — задаёт всё поведение), затем `kb:BASER-3`
(рыночное обоснование каждого пункта). Общий порядок чтения — [README](./README.md).

**Ты строишь** ядро материализации поверх виртуального дерева Nx devkit: чтение декларации → вычисление
плана → применение → модель владения. Плюс публикуешь интерфейс стратегии, который реализует `baser-modes`.

**Не твоё:** сами режимы (`packages/baser-modes`), CLI и гейт (`packages/baser-drift`), прогон в CI и
креды (ops-сторона, `kb:ADR-21`). Упёрся в чужую зону или в форму контракта — STOP, эскалация к architect.
