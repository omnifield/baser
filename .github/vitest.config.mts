import { defineConfig } from 'vitest/config';

/**
 * Пробы зоны `git` — обвязки репозитория.
 *
 * Зона живёт не в `packages`, поэтому своим проектом её делает этот файл:
 * без него код рядом с воркфлоу проверялся бы только живым прогоном
 * (`tasker:BASER2-167`).
 *
 * Расширение проб — `.mjs`, и по той же причине, что у гейта выпуска: скрипты
 * зоны не собираются, конвейер запускает тот самый файл, который лежит. Пробовать
 * сборку, которой нет, значит пробовать не то, что поедет.
 *
 * ВОРКФЛОУ ПРОБАМИ НЕ ПОКРЫВАЮТСЯ — это YAML, исполняемый чужим движком, и его
 * судит живой прогон. Здесь пробуется правило, которое воркфлоу зовёт.
 */
export default defineConfig(() => ({
  root: __dirname,
  cacheDir: '../node_modules/.vite/github',
  test: {
    name: 'git-pipeline',
    watch: false,
    globals: true,
    environment: 'node',
    include: ['scripts/**/*.spec.mjs'],
    reporters: ['default'],
  },
}));
