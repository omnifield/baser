# Add files here to ignore them from prettier formatting
/dist
/coverage
/.nx/cache
/.nx/workspace-data

# Шаблоны обвесов: парсера EJS у prettier нет, а форматировать их как JSON нельзя —
# внутри управляющие теги, и «исправление» сломает шаблон.
*.ejs