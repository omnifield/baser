{
  "name": "@omnifield/baser-release",
  "version": "0.1.0-dev.2",
  "description": "Инструмент выпуска baser: гейт номеров — ломающее изменение не уедет в реестр патчем, включая предвыпускные номера",
  "license": "MIT",
  "type": "module",
  "main": "./lib/index.mjs",
  "bin": {
    "baser-release-guard": "./bin/release-guard.mjs"
  },
  "exports": {
    "./package.json": "./package.json",
    ".": "./lib/index.mjs"
  },
  "files": [
    "bin",
    "lib",
    "!**/*.spec.mjs"
  ],
  "publishConfig": {
    "access": "public"
  },
  "nx": {
    "targets": {
      "typecheck": {
        "//": "Инструмент не собирается, поэтому проверка типов здесь — не сборка деклараций, а `tsc` по JSDoc (`checkJs`). Плагин типового пакета такую форму отключает («noEmit»), и цель объявлена явно, чтобы `pnpm verify` не терял зону молча.",
        "executor": "nx:run-commands",
        "cache": true,
        "inputs": [
          "{projectRoot}/**/*.mjs",
          "{projectRoot}/tsconfig.json",
          "{workspaceRoot}/tsconfig.base.json"
        ],
        "options": {
          "cwd": "{projectRoot}",
          "command": "tsc -p tsconfig.json"
        }
      }
    }
  }
}
