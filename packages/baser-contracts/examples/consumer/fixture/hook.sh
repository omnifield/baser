#!/bin/sh
# Хук набора consumer-kit — фикстура формы, а не настоящая проверка.
#
# Артефакт объявлен исполняемым ("executable": true, форма 6): он ПРОГРАММА, а не
# данные. Как именно бит ляжет на диск — обещание порта, который пишет, а не
# формы: форма выражает намерение автора обвеса, и только его.
set -eu

exec pnpm exec fixture --scope "*"
