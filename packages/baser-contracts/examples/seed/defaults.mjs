/**
 * Вычисляемые дефолты обвеса-фикстуры — СИНХРОННЫЕ чистые функции локального
 * контекста.
 *
 * Модуль принадлежит пакету обвеса; станок его только зовёт и не знает, что тот
 * считает, — как патрон не знает про фрезу.
 */

/** Владелец заготовки = имя репозитория: человек не вводит ничего. */
export function repoName(ctx) {
  return ctx.repo.name;
}
