{
  "name": "@fixture/kit",
  "version": "1.0.0",
  "baser": {
    "formVersion": 7,

    "source": {
      "id": "fixture/kit",
      "title": "Фикстура: обвес, у которого есть все слова формы",
      "contentRoot": "template"
    },

    "warningFrom": "./warnings.mjs#hookNotWired",

    "settings": {
      "name": {
        "title": "Имя набора",
        "description": "Считается из имени репозитория — пользователь не вводит ничего.",
        "type": "string",
        "defaultFrom": "./defaults.mjs#nameFromRepo"
      },
      "stamp": {
        "title": "Отметка выпуска",
        "description": "Личность и версия обвеса. Версия приезжает из манифеста пакета и бывает отсутствующей.",
        "type": "string",
        "defaultFrom": "./defaults.mjs#sourceStamp"
      },
      "command": {
        "title": "Команда прогона",
        "description": "Внутри есть кавычка — на ней видно, что подстановка не экранирует.",
        "type": "string",
        "default": "pnpm exec fixture --scope \"*\""
      },
      "retries": {
        "title": "Сколько раз повторить",
        "type": "number",
        "default": 2
      },
      "labels": {
        "title": "Метки набора",
        "type": "list",
        "default": ["form", "probe"]
      },
      "tools": {
        "title": "Инструменты прогона и их опции",
        "description": "Карта «ссылка → опции». Опции плоские и скалярные — глубже целевые спеки не ходят, поэтому и форма не ходит.",
        "type": "map",
        "of": "map",
        "default": { "fixture/lint": {} }
      },
      "env": {
        "title": "Переменные окружения прогона",
        "description": "Не задано (null) — блока в артефакте нет вовсе. Пустая карта ({}) — это «переменных нет», сказанное значением, а не умолчанием.",
        "type": "map",
        "of": "string",
        "default": null
      },
      "store": {
        "title": "Том под кэш прогона",
        "description": "Не задано — блок с томами не разворачивается вовсе.",
        "type": "string",
        "default": null
      },
      "strict": {
        "title": "Останавливаться на первой ошибке",
        "type": "boolean",
        "default": false
      }
    },

    "presets": {
      "shared": {
        "title": "Общий прогон: том под кэш, свои инструменты и остановка на первой ошибке",
        "values": {
          "store": "fixture-store",
          "strict": true,
          "tools": {
            "fixture/lint": { "strict": true },
            "fixture/probe": { "version": "0.11" }
          }
        }
      }
    },

    "layout": [
      {
        "src": "kit.json.ejs",
        "dest": "fixture/kit.json"
      },
      {
        "src": "kit.lock.json",
        "dest": "fixture/kit.lock.json",
        "render": false
      },
      {
        "src": "hook.sh.ejs",
        "dest": "fixture/hook.sh",
        "executable": true
      }
    ]
  }
}
