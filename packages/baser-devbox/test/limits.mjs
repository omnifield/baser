/**
 * ЛИМИТ ПРОБЫ, КОТОРАЯ ЗОВЁТ НАСТОЯЩИЙ ПАКЕТНЫЙ МЕНЕДЖЕР — ЧИСЛО С ПРИЧИНОЙ.
 *
 * Часть проб этой зоны изображает ПОСТСОЗДАНИЕ КОНТЕЙНЕРА: берёт шаг из артефакта,
 * который положила дверь, и исполняет его настоящим `sh` — а внутри шага живёт
 * настоящий `pnpm`/`npm`. Это не наш код и не наши миллисекунды: там разворачивается
 * пин `packageManager`, читается стор, ходит запрос в реестр. Мерить такую пробу
 * дефолтом vitest нельзя — 5000 мс выбраны им для проб над чистыми функциями.
 *
 * ── ЗАМЕР, А НЕ ГЛАЗОМЕР ────────────────────────────────────────────────────
 *
 * Мерить надо НА ЗАГРУЖЕННОЙ МАШИНЕ, и это главное здесь. На свободной машине та же
 * проба стоит 1.3 с — и ровно эта цифра создавала ощущение запаса, которого не было:
 * в конвейере рядом бегут все девять проектов, и запас съедает сосед, а не мы.
 * Замер сделан прогоном `pnpm verify` целиком (4 ядра, 2026-08-08, `tasker:BASER2-256`),
 * пять прогонов подряд — то есть в том же положении, в каком проба живёт в конвейере:
 *
 * | проба                                        | свободно | под полным прогоном  |
 * | -------------------------------------------- | -------- | -------------------- |
 * | `registry`: команды из сообщения снимают стену | 1.2 с   | 2.8 · 3.1 · 4.1 · 5.1 · **9.6 с** |
 * | `post-create-install`: дефолтная установка     | 1.7 с   | 3.1 · 3.4 · **4.3 с** |
 * | `registry`: реестр есть, доступа нет           | 0.7 с   | 1.9 · 2.5 · **3.1 с** |
 * | `manager-noise`: ловушка на вызов npm          | 0.8 с   | 2.0 · **2.5 с**      |
 *
 * Дефолт 5000 мс держался на том, что рядом никто не шумит: приёмка зоны `registry`
 * стала поднимать настоящие магазины (`tasker:BASER2-254`), машина стала медленнее — и
 * главная ветка покраснела на пробе, которую никто не ломал. Красное было стабильным,
 * а не флейком: перезапуск давал тот же отказ.
 *
 * ── ПОЧЕМУ 60 000 ───────────────────────────────────────────────────────────
 *
 * Разброс самой дорогой пробы между прогонами — 2.8 → 9.6 с, то есть в 3.4 раза, и
 * это разброс НАГРУЗКИ, а не нашего кода. Порог, у которого запас меньше разброса,
 * перестаёт означать «сломано» и начинает означать «не повезло» — на этом уже
 * краснели `tasker:BASER2-189` и `tasker:BASER2-156`, и это третий случай подряд.
 * Поэтому запас берётся ШЕСТИКРАТНЫЙ к худшему замеру (9.6 с), а не полуторный.
 *
 * 60 000 мс — это НЕ разрешение пробам быть медленными: дороговизну видно
 * длительностью в отчёте, а порог отвечает на другой вопрос — «проба ЗАВИСЛА».
 * Ответ на него не обязан быть тесным, он обязан быть однозначным. Тем же числом и по
 * той же причине живёт зона `pack`, где пробы так же зовут настоящий менеджер.
 *
 * ── ПОЧЕМУ ТОЧЕЧНО, А НЕ В КОНФИГЕ ЗОНЫ ─────────────────────────────────────
 *
 * У зоны 208 проб, и подавляющее большинство — раскладка артефакта без единого
 * процесса: 8–85 мс под той же нагрузкой. Общий порог в `vitest.config.mts` спрятал бы
 * зависание любой из них на целую минуту, то есть поменял бы один тихий отказ на
 * другой. Лимит ставится ИМЕНЕМ на пробу, которая за него платит, — и по имени видно,
 * за что: проба зовёт настоящий менеджер. Правило целиком — `kb:BASER3-37`.
 *
 * Точечность здесь не заявлена, а проверена (способ оттуда же): временная подмена на
 * `{ timeout: 1 }` роняет ровно помеченные пробы и не трогает соседние.
 */

/** Порог для пробы, исполняющей настоящий `pnpm`/`npm`. Причина — выше, замер — там же. */
export const MANAGER_PROBE_MS = 60_000;
