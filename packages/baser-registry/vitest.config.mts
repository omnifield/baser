import { defineConfig } from 'vitest/config';

export default defineConfig(() => ({
  root: __dirname,
  cacheDir: '../../node_modules/.vite/packages/baser-registry',
  test: {
    name: '@omnifield/baser-registry',
    watch: false,
    globals: true,
    environment: 'node',
    include: ['{src,tests}/**/*.{test,spec}.{js,mjs,cjs,ts,mts,cts,jsx,tsx}'],
    reporters: ['default'],

    // МЕСТО МАГАЗИНА — ВРЕМЕННОЕ НА ВЕСЬ ПРОГОН, а не на усмотрение пробы.
    //
    // Дефолт магазина — домашний каталог человека. Проба, забывшая назвать своё
    // место, поднимает раздачу там и оставляет чужой склад с пробными пакетами;
    // так и вышло при переезде на уровень локации, и убирать пришлось руками.
    // Разбор — в самом файле подготовки.
    globalSetup: ['./src/lib/isolated-home.setup.ts'],

    // ПОРОГ ВЫБРАН ПОД ПРОБЫ, КОТОРЫЕ ПОДНИМАЮТ НАСТОЯЩИЙ МАГАЗИН.
    //
    // Приёмка зоны держится на исполнении, а не на наличии файлов: проба
    // стартует отдельный процесс раздачи, ждёт от него ответа по HTTP, публикует
    // в него настоящим `npm` и ставит оттуда. Это не наш код и меряется он
    // секундами. Дефолт vitest (5000 мс) выбран для проб над чистыми функциями,
    // и запас до него у нас меньше разброса нагрузки на общем раннере — такой
    // порог перестаёт означать «сломано» и начинает означать «не повезло»
    // (тот же счёт зона двери оплатила красным CI, `tasker:BASER2-189`).
    //
    // 30 000 мс — тот же порог, что у двери, и по той же причине: он отвечает не
    // на вопрос «медленно ли», а на вопрос «зависло ли». Дороговизна видна
    // длительностью файла в отчёте, и прятать её порогом здесь нечем.
    testTimeout: 30_000,

    // Хуки поднимают и гасят магазин между пробами — та же цена, тот же порог.
    hookTimeout: 30_000,

    coverage: {
      reportsDirectory: './test-output/vitest/coverage',
      provider: 'v8' as const,
    },
  },
}));
