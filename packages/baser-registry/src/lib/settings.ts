/**
 * НАСТРОЙКИ МАГАЗИНА — объявлены здесь, заполняются человеком, дефолт наш.
 *
 * Модель канонная (`kb:BASER3-6`): инструмент объявляет список настроек и дефолт
 * к каждой, человек заполняет свой файл — столько полей, сколько захотел, — а
 * подставляем мы. Отсюда два правила, которые здесь держатся формой:
 *
 * - **не заполнил — работает дефолт**, и с выпуском инструмента дефолт поднимается;
 * - **заполнил — не поднимется никогда**: в файл человека мы не пишем, подниматься
 *   физически неоткуда.
 *
 * ФАЙЛ РОЖДАЕТСЯ ОДИН РАЗ, И ЗНАЧЕНИЙ В НЁМ НЕТ. `up` в локации без файла кладёт
 * его — с описаниями и ЗАКОММЕНТИРОВАННЫМИ дефолтами. Записать туда посчитанное
 * значение значило бы молча заморозить его: инструмент выпустился бы с новым, у
 * человека остался бы вчерашний, и он бы даже не узнал, что что-то выбирал.
 * Пустой файл — рабочее состояние, а не пробел.
 *
 * АДРЕС И ПОРТ — НАСТРОЙКА, А НЕ ХАРДКОД, и это требование ТЗ, а не вкус: порт
 * магазина внутри контейнера может столкнуться с чем угодно, что человек там уже
 * держит, и единственный легальный ход в таком случае — поменять настройку
 * (`kb:BASER3-5`). Наружу хост-машины при этом не торчит ничего: единственная
 * публикация во всей системе — дверь `:8080` (`kb:FUND-5`), магазин остаётся
 * внутренним.
 */

import { resolve } from 'node:path';
import { parse } from 'yaml';
import type { ShopProblemLog } from './problems.js';

/** Значения, с которыми магазин работает. Всё поле обязательно — дефолт есть у каждого. */
export interface ShopSettings {
  /** Порт, на котором слушает раздача. */
  readonly port: number;
  /**
   * Интерфейс, на котором слушает раздача.
   *
   * ДЕФОЛТ `0.0.0.0` — РАЗДАЧА ВИДНА СОСЕДЯМ ПО СЕТИ ЛОКАЦИИ, и довод здесь не
   * про удобство. Магазин заводят ради одной работы — отдать товар соседу;
   * дефолт, при котором эта работа не делается, это не «настройка побезопаснее»,
   * а неисправность из коробки. Так и вышло живьём: потребитель отложил переезд
   * целиком, потому что его sandbox не мог взять у него ни одного пакета
   * (`tasker:BASER2-266`, решение — `tasker:BASER2-267`).
   *
   * НАРУЖУ МАШИНЫ НЕ ВЫХОДИТ НИЧЕГО. Хост-портов девконтейнер не публикует,
   * дверь наружу одна — `:8080` (`kb:FUND-5`); видимость расширяется ровно до
   * общей docker-сети и не дальше (`kb:BASER3-40`). Single-origin этим не
   * нарушается: речь про соседство внутри, а не про выход наружу.
   *
   * `127.0.0.1` дефолтом БЫЛ и снят замером architect'а: свою локацию мы
   * открыли руками и не заметили, что для всех остальных инструмент бесполезен.
   * Прежде дефолтом уже стоял `0.0.0.0` и был снят по ревью
   * (`tasker:BASER2-249`) с доводом «этого состояния никто не выбирал» — теперь
   * оно ВЫБРАНО и названо здесь, поэтому довод больше не применяется.
   *
   * Запереть раздачу в петле по-прежнему можно — значением `127.0.0.1`, то есть
   * осознанно, а не молчанием. Кому раздача видна на самом деле, магазин говорит
   * данными: `shop.reach` (`reachOf`).
   */
  readonly host: string;
  /**
   * Куда магазин пишет свой лог.
   *
   * Путь — настройка, а не хардкод, и это ПРИЧИНА, а не следствие: `shop.log` в
   * текущем каталоге оставляет мусор у каждого, кто позвал команду не из корня
   * (поймано ревью на живом следе, `tasker:BASER2-249`). Относительный путь
   * считается от КОРНЯ МАГАЗИНА, а не от каталога вызова и не от постройки:
   * лог принадлежит раздаче, а раздача — вещь локации.
   *
   * Дефолт лежит внутри папки магазина: лог — след прогона, и место у него там
   * же, где всё остальное наше.
   */
  readonly log: string;
  /**
   * Апстрим: у кого магазин спрашивает то, чего нет на своём складе.
   *
   * Прокси, а не второй реестр рядом (`kb:BASER3-9`): потребитель вписывает ОДИН
   * адрес, чего нет локально — тянется снаружи и кэшируется. Альтернатива (два
   * адреса и список, что откуда) возвращает человеку ручную маршрутизацию имён
   * — ровно то, ради устранения чего магазин и заводится.
   */
  readonly uplink: string;
}

export const DEFAULT_SETTINGS: ShopSettings = {
  // Порт verdaccio по умолчанию. Своего числа не выдумываем: человек, знающий
  // рынок, ждёт именно его, а незнающему всё равно.
  port: 4873,
  // Раздача отвечает соседям по сети локации. Магазин для того и заводят;
  // разбор — в описании поля выше. Наружу машины при этом не выходит ничего.
  host: '0.0.0.0',
  uplink: 'https://registry.npmjs.org/',
  // Путь от корня магазина. Совпадает с раскладкой (`layout.ts`) и обязан
  // совпадать: проба это проверяет, иначе два места разъедутся молча.
  log: 'runtime/shop.log',
};

/** Что человек вправе поменять — единственный перечень, из него же родится файл. */
interface SettingDeclaration {
  readonly key: keyof ShopSettings;
  readonly title: string;
  readonly description: string;
}

const DECLARATIONS: readonly SettingDeclaration[] = [
  {
    key: 'port',
    title: 'Порт раздачи',
    description:
      'На нём магазин слушает внутри контейнера. Меняют его, когда порт уже\n' +
      'занят чем-то своим: команда честно откажет с кодом address-busy, а не\n' +
      'займёт соседний молча. Наружу машины порт не публикуется ни при каком\n' +
      'значении — единственная дверь наружу это :8080.',
  },
  {
    key: 'host',
    title: 'Интерфейс раздачи — кому магазин отвечает',
    description:
      'Дефолт открывает раздачу СОСЕДЯМ по сети вашей локации, и это причина, а\n' +
      'не удобство: магазин заводят ровно для того, чтобы соседний контейнер\n' +
      'взял у вас пакет. Дефолт, при котором он этого не может, — неисправность\n' +
      'из коробки, а не осторожная настройка.\n' +
      'Сосед приходит по алиасу вашей локации и этому порту — например\n' +
      'http://<алиас локации>:4873. Сам алиас задаёт тот, кто поднимал\n' +
      'контейнер, а не магазин: своего имени в сети инструмент не знает и\n' +
      'выдумывать его не станет.\n' +
      'Наружу машины не выходит ничего ни при каком значении: хост-портов\n' +
      'контейнер не публикует, дверь наружу одна — :8080.\n' +
      'Запереть раздачу в петле можно значением 127.0.0.1 — тогда придёт только\n' +
      'эта локация, и status ответит reach: self-only.\n' +
      'Форма временная — связью контейнеров займётся отдельный продукт сетей, и\n' +
      'тогда значение будет выставлять он, а не вы руками (kb:BASER3-40).',
  },
  {
    key: 'log',
    title: 'Куда магазин пишет лог',
    description:
      'Единственный след упавшего старта: команда назовёт этот путь, а причину\n' +
      'вы прочитаете здесь. Относительный путь считается ОТ КОРНЯ МАГАЗИНА, а не\n' +
      'от каталога, из которого позвали команду, — иначе лог расползался бы по\n' +
      'дереву. Абсолютный путь берётся как есть.',
  },
  {
    key: 'uplink',
    title: 'Апстрим — у кого спрашивать то, чего нет на складе',
    description:
      'Магазин работает прокси: чего нет локально, он берёт наверху и кэширует,\n' +
      'поэтому потребителю хватает одного адреса. Пустым не бывает: без апстрима\n' +
      'локация теряет доступ ко всему, что не публиковали руками.',
  },
];

/**
 * Разбирает файл человека. Диска не трогает — текст приходит снаружи.
 *
 * `text === null` — файла нет, и это НЕ отказ: работают дефолты целиком.
 * Отказы копятся в журнал, а разбор идёт до конца: человек получает все свои
 * опечатки за один прогон, а не по одной за запуск.
 */
export function readSettings(
  text: string | null,
  at: string,
  problems: ShopProblemLog,
): ShopSettings {
  if (text === null) {
    return DEFAULT_SETTINGS;
  }

  let parsed: unknown;
  try {
    parsed = parse(text);
  } catch (error) {
    problems.add(
      'config-unreadable',
      at,
      `настройки магазина не разбираются как YAML: ${(error as Error).message}`,
    );
    return DEFAULT_SETTINGS;
  }

  // Пустой файл — законное состояние: ровно таким он и рождается.
  if (parsed === null || parsed === undefined) {
    return DEFAULT_SETTINGS;
  }

  if (typeof parsed !== 'object' || Array.isArray(parsed)) {
    problems.add(
      'config-not-mapping',
      at,
      'настройки магазина — это набор ключей; здесь лежит ' +
        (Array.isArray(parsed) ? 'список' : typeof parsed),
    );
    return DEFAULT_SETTINGS;
  }

  const raw = parsed as Record<string, unknown>;
  const known = new Set<string>(DECLARATIONS.map((one) => one.key));
  for (const key of Object.keys(raw)) {
    if (!known.has(key)) {
      problems.add(
        'setting-unknown',
        `${at}#${key}`,
        `настройки "${key}" у магазина нет; есть: ${[...known].join(' · ')}`,
      );
    }
  }

  return {
    port: readPort(raw['port'], at, problems),
    host: readText(raw['host'], 'host', at, problems),
    uplink: readUplink(raw['uplink'], at, problems),
    log: readText(raw['log'], 'log', at, problems),
  };
}

function readPort(
  value: unknown,
  at: string,
  problems: ShopProblemLog,
): number {
  if (value === undefined || value === null) {
    return DEFAULT_SETTINGS.port;
  }
  if (typeof value !== 'number' || !Number.isInteger(value)) {
    problems.add(
      'setting-type',
      `${at}#port`,
      `порт — целое число, а здесь ${describe(value)}`,
    );
    return DEFAULT_SETTINGS.port;
  }
  if (value < 1 || value > 65535) {
    problems.add(
      'port-out-of-range',
      `${at}#port`,
      `порт бывает от 1 до 65535, а здесь ${value}`,
    );
    return DEFAULT_SETTINGS.port;
  }
  return value;
}

function readText(
  value: unknown,
  key: 'host' | 'uplink' | 'log',
  at: string,
  problems: ShopProblemLog,
): string {
  if (value === undefined || value === null) {
    return DEFAULT_SETTINGS[key];
  }
  if (typeof value !== 'string' || value.trim() === '') {
    problems.add(
      'setting-type',
      `${at}#${key}`,
      `${key} — непустая строка, а здесь ${describe(value)}`,
    );
    return DEFAULT_SETTINGS[key];
  }
  return value.trim();
}

function readUplink(
  value: unknown,
  at: string,
  problems: ShopProblemLog,
): string {
  const text = readText(value, 'uplink', at, problems);
  if (text === DEFAULT_SETTINGS.uplink) {
    return text;
  }
  // Адрес апстрима проверяется здесь, а не под первым запросом: непригодный
  // адрес иначе всплыл бы установкой, которая «почему-то не находит пакет».
  try {
    new URL(text);
  } catch {
    problems.add(
      'setting-type',
      `${at}#uplink`,
      `uplink — адрес реестра целиком, вместе со схемой; "${text}" на него не похож`,
    );
    return DEFAULT_SETTINGS.uplink;
  }
  return text;
}

function describe(value: unknown): string {
  if (value === null) return 'null';
  if (Array.isArray(value)) return 'список';
  return `${typeof value} (${JSON.stringify(value)})`;
}

/**
 * АДРЕС, ПО КОТОРОМУ В МАГАЗИН ХОДЯТ, — не то же самое, что интерфейс, который
 * он слушает.
 *
 * `0.0.0.0` — это «слушаю на всех», и ходить по нему нельзя: как адрес назначения
 * он не означает ничего осмысленного. Подставить его в `--registry` значит выдать
 * человеку строку, которая где-то сработает, а где-то нет, — поэтому здесь он
 * переводится в петлевой адрес, по которому ходит тот же контейнер.
 *
 * Соседу по docker-сети нужен третий адрес — имя контейнера, — и магазин его не
 * знает и знать не может: имя даёт тот, кто контейнер поднимал. Врать про него
 * мы не будем; называем то, что верно всегда, а рядом честно говорим, придёт ли
 * сосед вообще (`reachOf`). Адрес, верный только хозяину, без такой пометки и
 * был дефектом (`tasker:BASER2-259`).
 */
export function clientAddress(settings: ShopSettings): string {
  const host =
    settings.host === '0.0.0.0' || settings.host === '::'
      ? '127.0.0.1'
      : settings.host;
  return `http://${host}:${settings.port}`;
}

/** Адрес, который слушает процесс: то, что уходит verdaccio как `--listen`. */
export function listenAddress(settings: ShopSettings): string {
  return `${settings.host}:${settings.port}`;
}

/**
 * КОМУ РАЗДАЧА ОТВЕЧАЕТ — единственное, что магазин про свою видимость знает
 * точно.
 *
 * `self-only` — слушается петля: придёт только эта же локация.
 * `network` — слушается не петля: сосед по сети локации придёт, и придёт по
 * порту раздачи.
 *
 * Имени, по которому он придёт, здесь нет намеренно. Алиас локации задаёт тот,
 * кто поднимал контейнер, а не магазин (`kb:BASER3-40`), и подставить выдуманный
 * значило бы выдать адрес, по которому спрашивающий не придёт, — ровно тот
 * дефект, из-за которого поле и заводится (`tasker:BASER2-259`). Мы называем
 * факт, который знаем, и молчим о том, которого не знаем.
 */
export type ShopReach = 'self-only' | 'network';

export function reachOf(settings: ShopSettings): ShopReach {
  const host = settings.host.trim().toLowerCase();
  // Петля — это СЕТЬ 127.0.0.0/8, а не один адрес: `127.0.0.2` тоже своя. И
  // `localhost` сюда же — человек пишет его чаще, чем цифры.
  const loopback =
    host === 'localhost' ||
    host === '::1' ||
    host === '[::1]' ||
    /^127\./.test(host);
  return loopback ? 'self-only' : 'network';
}

/**
 * Куда на самом деле ляжет лог.
 *
 * Относительный путь считается от КОРНЯ МАГАЗИНА, а не от каталога вызова и не
 * от постройки. Команда ставится глобально и зовётся откуда угодно; лог,
 * посчитанный от текущего каталога, оставлял бы файл там, где человек случайно
 * стоял (ровно тот мусор, который поймало ревью первого коммита), а посчитанный
 * от постройки — клал бы след раздачи внутрь клона, из которого её сегодня
 * позвали, хотя раздача принадлежит локации.
 */
export function logPath(settings: ShopSettings, shopRoot: string): string {
  return resolve(shopRoot, settings.log);
}

/**
 * Текст файла настроек, каким он рождается: описания и закомментированные дефолты.
 *
 * Формат YAML выбран ради человека — файл заполняет он, значит ему нужны
 * комментарии, и имя с описанием каждой настройки обязаны лежать в файле,
 * который он открывает, а не в доке, которую он не открывает (`kb:BASER3-6`).
 */
export function settingsTemplate(): string {
  const head = [
    '# Раздача пакетов этой ЛОКАЦИИ — настройки @baser/registry.',
    '#',
    '# Магазин принадлежит локации (контейнеру), а не отдельной постройке: порт',
    '# и процесс всё равно одни на весь контейнер, поэтому склад и эти настройки',
    '# лежат рядом с ними, а не внутри одного из клонов. Две постройки в одной',
    '# локации пользуются одной раздачей и одним складом — как и одним участком.',
    '#',
    '# Файл ваш: инструмент его читает и никогда в него не пишет. Не заполнено —',
    '# работает дефолт, и с новым выпуском инструмента дефолт поднимется сам.',
    '# Заполнено — останется вашим навсегда.',
    '#',
    '# Значения ниже закомментированы намеренно: это ДЕФОЛТЫ на день рождения',
    '# файла, а не ваш выбор. Раскомментировав строку, вы замораживаете значение.',
    '#',
    '',
  ];

  const body = DECLARATIONS.flatMap((one) => [
    `# ${one.title}`,
    ...one.description.split('\n').map((line) => `# ${line}`),
    `# ${one.key}: ${JSON.stringify(DEFAULT_SETTINGS[one.key])}`,
    '',
  ]);

  return [...head, ...body].join('\n');
}
